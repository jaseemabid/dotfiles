#!/bin/bash

# Return the current volume status of your system in a for xmobar

status=`amixer sget Master,0`
if [[ $status == *"off"* ]]; then
    echo " "
else
    p=`amixer sget Master,0 | grep Right\: | awk '{print $5}' | tr '\[\]' ' '`
    echo "" $p
fi
